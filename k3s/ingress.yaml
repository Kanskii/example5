apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example5
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: kanat.compnet.kz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: example5
                port:
                  number: 80
    - host: kanat.compnet.kz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: example5
                port:
                  number: 80